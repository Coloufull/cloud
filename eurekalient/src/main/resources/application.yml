server:
  port: 8010
spring:
  application:
    name: provider
eureka:
  client:
#    register-with-eureka: false
#    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
#    instance-id: localhost:8010
